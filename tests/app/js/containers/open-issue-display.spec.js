/* @flow */
'use strict'

import { describe, it } from 'mocha'
import expect from 'expect'

describe('Open Issues Display', () => {

  // #NOTE the filteredOffset/sortby reducer might need to check what view is open
  // and might need not to change no data view is open

  // pass over a number

  // should load this number from currecty selected range to view

  // needs the final number selector? range selector?

  // needs graph range reducer


})
