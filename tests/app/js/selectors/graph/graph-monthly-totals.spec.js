/* @flow */
'use strict'

import { describe, it, beforeEach } from 'mocha'
import expect from 'expect'

describe('Graph Monthly Totals Selector', () => {
  // it should return a Map

  // Map should have a dates property

  // dates property is a list of strings

  // Map should have a totals property

  // totals property should be a List of Maps

  // dates and totals should be the same lenght
})
